import { FC } from 'react';
import { AppHeaderUI } from '@ui';

export const AppHeader: FC = () => <AppHeaderUI userName='' />;
/** TODO: подключить юзера*/
// userName={useSelector((state) => state.user.user?.name)}
